# Generated by the lesspaper-ngl installer. Ports and optional extra GID only.
# Do not store secrets here.
#
# Placeholders:
#   {{BIND}} {{HTTP_PORT}}
#   {{API_PORTS_BLOCK}}
#   {{GROUP_ADD_API}} {{GROUP_ADD_WORKER}}
#
# The installer writes the rendered file via config_write_override.
services:
  web:
    ports:
      - "${LESSPAPER_NGL_BIND}:${LESSPAPER_NGL_HTTP_PORT}:80"
